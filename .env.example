# Non-sensitive configuration
NEW_RELIC_ACCOUNT_ID=
DASHBOARD_GUID=
SECRETS_PROVIDER=aws  # aws | vault | azure

# Reporting
REPORT_TIMEZONE=US/Eastern
EVENT_NAME=Weekend Performance Report
DASHBOARD_URL=
REPORT_USER_NAME=SRE Automation

# Delivery
EMAIL_RECIPIENTS=team@example.com

# AWS (if SECRETS_PROVIDER=aws)
AWS_REGION=us-east-1

# Vault (if SECRETS_PROVIDER=vault)
VAULT_ADDR=
VAULT_TOKEN=
VAULT_MOUNT=secret

# Azure (if SECRETS_PROVIDER=azure)
AZURE_KEY_VAULT_URL=

# Secret IDs (override if your org uses different paths)
SECRET_ID_NEW_RELIC_API_KEY=prod/newrelic/api-key
SECRET_ID_SLACK_WEBHOOK=
SECRET_ID_O365_CREDENTIALS=
